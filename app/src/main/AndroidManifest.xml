<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/icono"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icono"
        android:supportsRtl="true"
        android:theme="@style/Theme.Public_transport_android.ActionBar"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".ui.Pasajero.PasajeroMainActivity"
            android:exported="false"
            android:label="@string/title_activity_pasajero_main"
            android:theme="@style/Theme.Public_transport_android.ActionBar" />
        <activity
            android:name=".ui.Roles.Base.UnidadAdapter"
            android:exported="false" />
        <activity
            android:name=".ui.Roles.Parada.HomeParadaActivity"
            android:exported="false"
            android:label="@string/title_activity_home_parada"
            android:theme="@style/Theme.Public_transport_android.ActionBar" />
        <activity
            android:name=".ui.Roles.Base.HomeBaseActivity"
            android:exported="false"
            android:label="@string/title_activity_home_base"
            android:theme="@style/Theme.Public_transport_android.ActionBar" />
        <activity
            android:name=".HomeActivity"
            android:exported="false"
            android:label="@string/title_activity_home"
            android:theme="@style/Theme.Public_transport_android.ActionBar" />
        <activity
            android:name=".Login"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>